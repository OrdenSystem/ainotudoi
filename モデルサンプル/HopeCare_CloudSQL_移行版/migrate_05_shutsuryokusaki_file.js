/**
 * [RETIRED] 出力先ファイル データ移行スクリプト
 *
 * ============================================================
 *  退役日: 2026-04-23
 * ============================================================
 * CloudSQL への「出力先ファイル」テーブル移行（18列、ソース: AppSheet Database
 * からエクスポートしたCSVファイル）は完了済みのため、本スクリプトの中身は
 * 退避しました。
 *
 * 前提: 帳票マスタ複製登録 が先に移行済み（FK制約）
 *
 * 元のコードは GAS のバージョン履歴から復元可能です。
 *   Version 1: "Before stage1 cleanup - remove migration and test scripts"
 *
 * 一定期間運用に問題がなければ、将来的に本ファイル自体を完全削除予定。
 */
