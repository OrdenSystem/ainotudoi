/**
 * [RETIRED] 請求完了処理（相談記録IDリストによる CSV バックアップ＋フラグ更新）
 *
 * ============================================================
 *  退役日: 2026-04-23
 * ============================================================
 * 表示中の相談記録IDリストを受け取り、CSV 形式でバックアップを出力した上で、
 * 該当レコードの請求完了フラグを一括更新する処理。旧版はスプレッドシートを対象に
 * 動作していた。
 *
 * 退役理由:
 *   - 運用は CloudSQL 版 `請求完了処理_CloudSQL.js` に完全移行済み
 *   - 請求アプリ (請求_Hope Care DX_とよさと_ver2.0) からは新版
 *     completeBillingWithIDs_CloudSQL が呼ばれている
 *   - 旧版関数 (completeBillingWithIDs / saveRowsToCsv) はどこからも呼ばれて
 *     いないことを確認済み
 *   - 対象は CloudSQL 上の `01相談記録` テーブル
 *
 * 元のコードは GAS のバージョン履歴から復元可能です。
 *   Version 2: "Before stage2 cleanup - retire legacy non-CloudSQL scripts"
 *
 * 一定期間運用に問題がなければ、将来的に本ファイル自体を完全削除予定。
 */
